# Week 3 執行總結：Gemma 3 + Shadow-FT + LoRA 參數掃描

> **三句話總結**：Week 2 的崩潰**不是**超參數開太大造成的——scaling 從 0.5 掃到 20 全都不崩，
> 這推翻了事前認為最可能的假設；Shadow-FT 的前提（σ < 0.05）在 gemma-3-4b 上實測不成立
> （σ = 0.107），三條假設 S1–S3 全數否證，是一個完整的負結果；MoE 方面繁簡對照證實
> 「中文專家」是**語言**專家而非字符集專家（繁簡 KL 僅中英的 5.5%），路由微調提案的立論成立。
>
> 所有數字：`results/week3/`、`reports/week3/`；流程：`notebooks/week3_colab.ipynb`；
> 驗收明細：`week3_執行手冊.md`。

---

## 1. Week 2 交接的五個關鍵事實（本週實驗的前提）

1. mlx-lm 的 LoRA `scale` 是**直接乘數** → Week 2 有效 scaling = 20，業界常規（2.0）的 10 倍。
2. E4B 後 18 層共用 KV → `k_proj`/`v_proj` 的 adapter 被**靜默略過**，實際只訓了 q、o。
3. 中英路由差異（E4）訊噪比 6.6×；雜訊底線必須用 bootstrap 實測，解析式低估 3.5 倍。
4. 準確率 macro / micro 並列：Week 2 跌幅 −17.4 pt（科目平均）／−21.5 pt（逐題平均）。
5. MLX 4-bit 實測 4.50 bit/參數（group 64）；不同 group size 的「4-bit」差 5.6%。

**本週新增的一條**（Step 1 checkpoint 掃描）：Week 2 的格式崩潰是**漸進累積**的——
無法解析率 200→1000 步為 23.7% → 11.5% → 32.6% → 39.7% → 40.4%，400 步後加速。

---

## 2. 框架對照（答 Q1：記憶體優化省在哪、數學有沒有變）

同一組 LoRA 訓練（gemma-3-4b-it、4-bit、r=16、30 步）在三個框架各跑一次：

| 框架 | 峰值 GiB | s/step | 末 loss | 判定 |
|---|---:|---:|---:|---|
| HF+peft fp16 | 6.39 | （1.81） | 0.0 | ❌ 溢位崩潰，如預測（Gemma 3 activation > fp16 上限） |
| HF+peft fp32 | 8.00 | 7.52 | 1.5702 | ✅ 教科書基準線，能跑但慢 |
| Unsloth | 7.50 | 2.02 | 1.8211 | ✅ 快 **3.7×**（預測 3–5×） |

**三個結論**：

1. **T4 上 Unsloth 不是選擇是必需**：fp16 必崩、fp32 慢 3.7 倍。
2. **loss 大致等價、不完全等價**：末值差 16%（超過 10% 門檻），後半段平均差 6.9%（過門檻）。
   差異來源候選：optimizer 不同（adamw_torch vs adamw_8bit）、精度路徑不同。引用時兩個數字並報。
3. **Unsloth 省的不是 logits**：只比 fp32 省 0.50 GiB，遠小於 logits+CE 落地的 1.50 GiB
   → fused cross-entropy 在 Gemma 3 上**沒有生效**，省的是 Triton fused kernel 與重寫的梯度檢查點。

---

## 3. LoRA 參數掃描（答 Q2：Week 2 是不是超參數的鍋）

固定 r=16、all-linear、200 步，只動 scaling（快速評測，每科 100 題）：

| | A1 (0.5) | A2 (1.0) | A3 (2.0 常規) | A4 (4.0) | A5 (20 = Week 2 重現) |
|---|---:|---:|---:|---:|---:|
| 無法解析率 | 18.3% | 21.7% | 17.3% | 16.0% | **12.7%** |
| 嚴格正確率 | 31.0% | 29.7% | 29.7% | 33.0% | 33.7% |

事前寫下的預測是「無法解析率隨 scaling 單調上升；A5 重現 Week 2 的 ~40% 崩潰；A3 < 5%」。
**實測全部落空**：A5 沒崩（12.7%，反而最低）、A3 沒有 < 5%、方向整體與預測相反。

**判讀**：落在事前預案的第三分支——**「全都不崩」→ Week 2 崩潰的主因不是超參數**。
剩下的候選：Gemma 4 thinking-channel 模板的處理、訓練步數（checkpoint 掃描顯示 400 步後加速崩）、
twinkle-eval 無種子洗牌帶來的評測雜訊。這是本週最重要的意外發現，下週優先查。

**Stage B（rank 8→64）**：正確率 29.0% → 33.0% 輕微上升，與 Shadow-FT 論文
「常規 LoRA rank 越大越傷」相反，但差距多在 300 題的雜訊內。
**Stage C（attn-only vs all-linear）**：30.0% vs 29.7%，無可辨差異（H6 補上下游指標）。

---

## 4. Shadow-FT（答 Q3/Q4：換個宿主訓練能不能救格式）

### 4.1 前提檢驗先不通過

Shadow-FT 的立論是 base 和 instruct 權重幾乎相同（論文：所有測過的模型 σ < 0.05）。實測：

| σ | 值 |
|---|---:|
| 全部張量（含 vision/projector） | 0.0942 |
| **僅 language_model（論文的算法）** | **0.1075** |

**門檻的 2 倍以上**，差最大的集中在多層 `mlp.down_proj`（σ 0.30–0.35）。
gemma-3-4b 的 it 相對 pt 改動幅度遠大於論文測過的模型家族。

### 4.2 2×2 結果（完整評測 1,036 題，嚴格 micro）

| | scaling 2 | scaling 20 |
|---|---:|---:|
| 常規 LoRA on it | A3：31.8%（無法解析 18.3%） | A5：34.3%（11.2%） |
| Shadow-FT（train on pt） | D1：34.1%（19.4%） | D2：23.1%（**45.7%**） |
| 未微調 base_it | **38.0%**（4.2%） | |

| 假設 | 判準 | 實測 | 判定 |
|---|---|---|---|
| S1 保住格式 | D1 無法解析 < 5% 且明顯低於 A3 | 19.4% vs 18.3% | ❌ |
| S2 知識增益 | D1 ≥ base_it | 34.1% < 38.0% | ❌ |
| S3 對超參數不敏感 | \|D1−D2\| ≪ \|A3−A5\| | 11.0 pt vs 2.5 pt（方向相反） | ❌ |

**判讀**：前提不成立 → 方法失效，兩者互相印證。**Shadow-FT 不適用於 gemma-3-4b 的 pt↔it**。
這與「論文沒測過這一對、而我們的 σ 檢驗先亮紅燈」一致——先驗前提的流程值得保留。

**附帶觀察**：本週所有微調（包含最保守的 A1）正確率都低於未微調的 base_it，
且無法解析率 2–5 倍於 base_it。在這份資料與 200 步的設定下，微調本身就是淨傷害；
「要不要繼續微調路線」應該作為下週的討論題。

---

## 5. MoE routing（細節見 `moe_routing_分析.md`）

Week 2 已證實：專家負載高度不均（cv 訊噪比 7.6×）、中英路由顯著不同（29/30 層 p<0.01）、
差異集中在極少數專家（平均 2.9 個涵蓋單層 80% KL）、微調 attention 就會讓路由漂移
（未掛層 KL 精確為 0）。

**本週完成 §5.1 前置作業並實測**（`scripts/router_prestudy.py`，不吃 GPU 配額）：
逐 prompt 存 counts、prompt 層級**窮舉 permutation test**（2¹⁰ = 1024 種配對標籤交換、精確 p）、
**繁簡對照**（簡體版逐字轉換、用詞不變，變因鎖在字符集）。三個結果：

| 對照 | 平均 KL | 訊噪比 | 全域 p | p<0.01 的層 |
|---|---:|---:|---:|---:|
| 繁體 vs 英文 | 0.3059 | 7.5× | 0.001 | 30/30 |
| 簡體 vs 英文 | 0.2964 | 7.2× | 0.001 | 30/30 |
| 繁體 vs 簡體 | **0.0169** | 1.9× | 0.001 | 14/30 |

1. **「中文專家」是語言專家，不是字符集/tokenization 專家**：繁簡 KL 只有中英的 **5.5%**，
   繁簡兩版各自選出的 top-3 專家 **87% 相同** → **§5 四個微調提案的立論保留**。
2. **E4 通過更嚴格的檢定**：prompt 層級 permutation 下中英仍 30/30 層 p<0.01
   → Week 2「token 不獨立、p 偏樂觀」的方法論缺口正式解除。
3. **字符集殘餘成分集中在前段層**（第 2–3 層佔 22–28%、後段 2–3%），
   支持「前段=字面層級、後段=語言層級」的 U 型解讀。
   附帶：split-half 穩定性 88%（門檻 60%）——10-prompt 材料選 top-3 專家已經夠穩，
   與事前預測相反；挑專家規則更新為「取繁簡兩版都入選的交集」。

---

## 6. 本週方法論教訓（值得帶去下週）

1. **前提要先驗**：σ 檢驗一小時，省掉把 Shadow-FT 當可行方案往下蓋的一整週。
2. **事前寫下預測與分支**：Stage A 三個分支事前寫好，「全都不崩」才能立刻被認出是重要訊號
   而不是「實驗失敗」。
3. **檢查要確定性**：資料洩漏檢查曾因 `set` 順序隨機而「上次過、這次炸」——改成全量、
   全文、確定性比對後發現 TMMLU+ 有跨科目重複題，已在生成端做題目層級排除。
4. **記憶體數字要減 baseline**：同 session 殘留會把 peak 灌水（每組 +1.26 GiB），
   已改成記錄 `peak_delta`；引用歷史數字只用各 session 首組。

---

## 7. 下週規劃（優先序）

1. **查 Week 2 崩潰真因**（Q2 的遺留）：用 Week 3 的乾淨評測器重評 Week 2 模型
   （排除 twinkle-eval 洗牌雜訊）→ 若還是崩，對照 thinking-channel 模板處理；
   checkpoint 曲線（400 步後加速）提示也檢查步數×scaling 的交互作用。
2. **MoE 接 §5.5 推論期偏置探針**（零訓練成本的因果檢驗）：語言專家立論已實測成立，
   對選中專家的 router logit 加偏置，看繁中↑／英文↓是否成立；並補翻譯配對平行語料複驗。
3. **微調路線的去留討論**：本週所有微調都輸給未微調 base_it，繼續投入前先回答
   「資料/步數/目標是不是根本不對」。
4. 若升級 Colab Pro：P2（Flash Attention on/off）、P1（bf16 基準線）補齊。

---

## 附：本週產出

| 檔案 | 內容 |
|---|---|
| `results/week3/`、`reports/week3/` | 11 組訓練、17 份評測、框架對照、σ（含 per-tensor 明細） |
| `reports/week3/week3_results.md` | 全部數字的自動彙整表 |
| `scripts/router_prestudy.py` | MoE 前置：逐 prompt counts + permutation + 繁簡對照 |
| `notebooks/week3_colab.ipynb` | 全流程（含防呆：session 殘留擋停、洩漏全文比對、HF 污染偵測） |
| `week3_執行手冊.md` | 精簡版手冊，checklist 依實測勾核 |
