# Week 1 執行總結

## Part A 

提案的三階段架構是：
```
Week 1              Week 2–3                收尾 + 延伸
讀機制 + 資料就緒  →  GPT-OSS 微調 + 消融  →  DiffusionGemma 對照 + 彙整
   ▲ 已完成
```

---

## A1. 精讀 Playbook 核心章節（4 章）

依提案 slide 7，Week 1 的讀書範圍是「記憶體、精度、DP/ZeRO、EP」四個主題，

| 章節 | 主題 | 對應提案 slide 4 的哪一列 |
|---|---|---|
| ch01 | 單 GPU 記憶體剖析、活化重算、梯度累積 | 第 1、2、3 列 |
| ch10 | 混合精度、Flash Attention、融合核心 | 第 4 列 |
| ch02 | 資料平行、ZeRO-1/2/3 | 第 5 列 |
| ch06 | 專家平行（EP） | 第 6 列 |

**每一章的筆記都分成兩部分**：上半是課本內容的整理，下半是一個
「🧪 對照我的實驗」小節——把公式套到 GPT-OSS 20B 的實際規格上算出預測值。

## A2. 把公式落到 GPT-OSS 20B：記憶體預測

| 項目 | 依公式算出 | 官方標示 |
|---|---:|---:|
| 總參數 | 20.91B | 20.9B ✅ |
| Active 參數 | 3.61B | 3.6B ✅ |

（每個 token 只走 4/32 個專家，用到層內參數的 15.3%）

### Q1：QLoRA 載入 20.9B 權重要多少？→ 12.8 GiB

這裡有一個容易算錯的地方：GPT-OSS 的 MXFP4 **不是「全部 4-bit」**。
官方 config 明確排除了 `self_attn` / `router` / `embed_tokens` / `lm_head`，
**只量化 MoE expert 權重**；而且 MXFP4 是 4 bit 加上每 32 個元素一個
共享指數位元組，等效 **4.25 bit** 而非 4 bit。

| 載入方式 | 每參數位元組 | 權重記憶體 |
|---|---:|---:|
| bf16 | 2.000 | 39.0 GiB |
| MXFP4（官方權重） | 0.658 | **12.8 GiB** |

### Q2：為什麼 LoRA 的優化器狀態可以忽略？→ 公式裡的 N 是「可訓練參數」

| | 可訓練參數 | 佔比 | 記憶體（16 bytes/參數） |
|---|---:|---:|---:|
| 全參數微調 | 20.91B | 100% | 312 GiB |
| LoRA r=16（attn） | 8.0M | 0.038% | **0.12 GiB** |

差 2,627 倍。**但這個結論有條件**：若把 LoRA 也掛到 MoE expert 上，
可訓練參數跳到 185M（因為有 32 專家 × 24 層 = 768 組矩陣），
成本變 2.76 GiB 就不能忽略了。

### Q3：active 3.6B 對活化記憶體意味著什麼？→ 活化跟著 3.6B 走，不跟著 20.9B 走

課本公式直接代入會得到 34.5 GiB，比權重還大 2.7 倍，明顯不合理。
原因是公式的常數是對「MHA + dense MLP」推導的，GPT-OSS 三處都不符合：
GQA（KV 只有 8 頭）、MoE（每 token 只過 4/32 專家）、
以及半數層是視窗僅 128 的滑動注意力。修正後：

| 情境 | 活化記憶體 |
|---|---:|
| 課本公式直接代入 | 34.5 GiB（高估） |
| 修正架構後，無 Flash Attention | 18.3 GiB |
| 修正後，有 Flash Attention | **5.6 GiB** |
| 修正後，Flash + 梯度檢查點 | **0.50 GiB** |

### 加總後：所需資源

| 組成（seq 2048, bs 1） | 不開檢查點 | 開檢查點 |
|---|---:|---:|
| 基礎權重（凍結） | 12.8 | 12.8 |
| LoRA 參數 + 梯度 + 優化器 | 0.12 | 0.12 |
| 活化 | 5.59 | 0.50 |
| 輸出 logits | 2.30 | 2.30 |
| CUDA context / 碎片 | 1.2 | 1.2 |
| **合計** | **22.0 GiB ⚠️** | **16.9 GiB ✅** |

→ **24GB 卡必須開梯度檢查點**，22.0 GiB 太貼邊必定 OOM。

## A3. 首次實測驗證
在本機 M4 Pro 上載入 GPT-OSS 20B 並生成 300 tokens，拿到第一組實測數字。

### H1 — 權重記憶體預測誤差 8.3%

| 項目 | 值 |
|---|---:|
| 預測（MXFP4 權重） | 12.81 GiB = **13.75 GB** |
| 實測（生成時峰值） | **14.89 GB** |
| 差異 | +1.14 GB（**+8.3%**）✅ |

峰值高出的 1.14 GB 由 KV cache、Metal buffer pool、logits 與框架開銷組成，
其中 KV cache 因為 GQA 只有 8 個 KV 頭，僅 48 KB/token（386 tokens 共 18 MB），
幾乎可以忽略。**誤差在 10% 以內，H1 成立。**

### E2 — 用 roofline 從物理上證明 MoE 只讀 active 參數

實測生成吞吐 **56.6 tok/s**。但「很快」不足以當證據，所以改用
**記憶體頻寬 roofline** 來論證：

解碼是記憶體頻寬受限的——每產生一個 token，就必須把該次用到的權重
從記憶體讀進運算單元一次。因此 `理論上限 = 頻寬 ÷ 每 token 讀取量`。

| 每 token 需讀取 | 位元組 |
|---|---:|
| active experts（4/32） | 1.27 GB |
| attention（未量化，bf16） | 1.27 GB |
| lm_head（未量化，bf16） | 1.16 GB |
| **MoE 合計** | **3.71 GB** |
| 假如是 dense（讀全部 32 專家） | 12.60 GB |

M4 Pro 官方記憶體頻寬 273 GB/s：

| | 理論上限 |
|---|---:|
| MoE | 73.6 tok/s |
| dense | 21.7 tok/s |
| **實測** | **56.6 tok/s** |

實測達 MoE 理論上限的 **77%**（扣掉 kernel 與排程開銷後屬正常水準），
同時是 dense 理論上限的 **2.62 倍**。


## A4. ch02 / ch06

**其一：在 LoRA 情境下，ZeRO-1 與 ZeRO-2 幾乎無效。**

ZeRO-1/2 切分的是優化器狀態與梯度，而這兩項只對可訓練參數生效。
LoRA 的可訓練參數只有 8.0M，等於整個 ZeRO-1/2 在切一個 0.119 GiB 的東西：

| 方法（2 卡） | 每卡記憶體 | 相對省下 |
|---|---:|---:|
| 單卡 QLoRA baseline | 12.92 GiB | — |
| ZeRO-1 | 12.88 GiB | 0.3% |
| ZeRO-2 | 12.87 GiB | 0.4% |
| ZeRO-3 / FSDP | 讓 bf16 base 塞進 2×24GB | 質變 |

課本預設全參數微調，沒有明說這件事。**只有 ZeRO-3 有意義**，
而且價值不在「省記憶體」，在「讓原本塞不下的配置變成塞得下」。

同理，LoRA 讓資料平行的通訊量從全參數微調的 41.8 GB/步
降到 **16 MB/步**（約 0.6 毫秒）——課本花整章講的梯度重疊與分桶技巧，
在本實驗中應該完全看不出差別。**而證明「看不出差別」本身就是有價值的結果**，
它量化了 LoRA 讓分散式訓練變簡單的程度。

**其二：MoE 的參數分布解釋了三件原本各自獨立的事。**

全部 expert 權重佔全模型 **91.4%**（每層內佔 96.8%）。這一個數字同時說明了：
為什麼只量化 expert 就能把 39.0 GiB 壓到 12.8 GiB；
為什麼 LoRA 掛到 expert 上會暴增 23 倍；
以及為什麼活化記憶體跟著 active 參數走。

## A5. 資料管線：六步驟全部跑通

| 步驟 | 結果 |
|---|---|
| 1 載入與確認 | 50,000 筆，MIT 授權，欄位確認完畢 |
| 2 清理 | 49,984（僅刪 16 筆過長樣本） |
| 2b 簡體殘留掃描 | 56 筆可疑（0.11%），已標記待人工抽查 |
| 3 去重（精確 + MinHash 近似） | 49,965 |
| 4 套 Harmony chat template | 通道格式驗證通過 |
| 5 抽樣與切分 | 8,000 → train 7,600 / val 400 |


**統計全部樣本在 GPT-OSS tokenizer 下的 token 長度：**

| mean | p50 | p90 | p95 | p99 | max |
|---:|---:|---:|---:|---:|---:|
| 1,081 | 1,087 | 1,562 | 1,713 | **2,033** | 3,207 |

→ **`max_seq_len = 2048` 涵蓋 99.1% 的樣本**，只截斷 457 筆，且無樣本超過 4096。

分布圖見 `reports/token_length_hist.png`。

## A6. 環境與工具鏈

| 程式 | 用途 |
|---|---|
| `prepare_data.py` | 一鍵重現整條資料管線，輸出統計 JSON |
| `predict_memory.py` | 由架構參數自動推算記憶體預測表 |
| `verify_load.py` | 載入驗證 + roofline 分析（H1 / E2） |
| `verify_env.py` | 環境驗收，逐項回報缺什麼、怎麼補 |
| `inspect_router.py` | 觀察 MoE 路由分布（Week 2 用） |

另有一份逐格執行的 notebook 供探索與 code review 使用。

過程中也處理掉幾個會拖慢進度的環境問題，一併記錄以免重蹈覆轍：
`HF_HUB_ENABLE_HF_TRANSFER` 在 huggingface_hub 1.0+ 已失效（正確參數是
`HF_XET_HIGH_PERFORMANCE`）；Xet 傳輸協定在慢速連線上會重組失敗，
需用 `HF_HUB_DISABLE_XET=1` 退回一般 HTTPS（已寫進 `prepare_data.py`
的自動退回機制）；以及 LM Studio 讀不懂 `hf download` 抓下來的
HF safetensors 格式，本機推論改用 mlx-lm 直接讀既有快取即可。

## A7. 產出清單

| 檔案 | 內容 |
|---|---|
| `notes/ch01,02,06,10.md` | 四章筆記 + 21 條可驗證假設 |
| `reports/memory_prediction.md` | 完整記憶體預測推導 |
| `reports/load_verification.md` | **H1 / E2 實測驗證與 roofline 分析** |
| `reports/data_stats.json` | 資料管線每一步的筆數與統計 |
| `reports/token_length_hist.png` | Token 長度分布圖 |
| `reports/sample_rendered.txt` | 一筆完整 Harmony 格式樣本 |
| `scripts/*.py` | 五支工具程式 |
| `notebooks/01_data_pipeline.ipynb` | 逐格執行版資料管線 |
| `data/train`, `data/val` | 7,600 / 400 筆，可直接餵訓練器 |
| `datasets/ikala__tmmluplus/` | 66 個科目的評測資料 |

## A8. Baseline 評測：進行中

評測環境已完全打通——TMMLU+ 66 個科目下載完成，`mlx_lm.server` 提供
OpenAI 相容端點，Twinkle Eval 設定檔已寫好並通過驗證。

**目前策略是先跑 3 科試水溫**（台灣地理、台語、三民主義），
確認流程正確與時間成本後再擴大。原因是 gpt-oss 屬推理模型，
每題會先產生 analysis 通道的思考內容才給答案，在 56.6 tok/s 之下
單題耗時遠高於一般模型，貿然全科開跑可能要跨夜。

**科目選取原則**：
選台灣在地情境與繁中語言能力相關的科目，避開純理工科目——
後者考的是學科知識，不是繁中或台灣情境，放進來會稀釋掉這個實驗要measure的東西。
正式版建議的 10 科為：台灣地理、台語、三民主義、全民國防、行政法、
法學緒論、稅法、國中國文、技職國文、中國語文。

**可比性須註明**：本機 Metal 量到的準確率跨硬體可比，
但吞吐與峰值記憶體不能和 Week 2 的 CUDA 卡並列。

---

## A9. 租卡建議（由預測值推導，且 H1 已用實測佐證）：

| 卡 | 能做什麼 |
|---|---|
| 24GB（4090） | QLoRA，**且必須開梯度檢查點**（16.9 GiB） |
| **48GB（A6000/L40S）← 建議** | QLoRA 與 bf16 LoRA 都能跑，消融空間足夠 |
| 2×48GB | 額外實作 DP / ZeRO 章節，只需租數小時 |

---

## A10. 交接給 Week 2 的確定事項

以下設定已由 Week 1 的量測定案，Week 2 直接沿用：

| 項目 | 值 | 依據 |
|---|---|---|
| `max_seq_len` | 2048 | token 長度 p99 = 2,033，涵蓋 99.1% |
| `reasoning_effort` | `medium` | 訓練與評測必須同檔位 |
| 隨機種子 | 42 | 貫穿 shuffle / split / sampling |
| 訓練資料 | `data/train` 7,600 筆 | `python scripts/prepare_data.py` 可完全重現 |
| 梯度檢查點 | 24GB 卡必開 | - |f

**21 條可驗證假設**（每條都有預測值與量測方式，詳見各章筆記）：

| 來源 | 編號 | 主題 | 狀態 |
|---|---|---|---|
| ch01 | H1–H6 | 權重記憶體、LoRA 優化器、檢查點取捨、logits 成本、活化成長律、expert LoRA | **H1 ✅ 已證實** |
| ch10 | P1–P4 | MXFP4 vs bf16 記憶體、Flash Attention 效益、平方項來源、QLoRA 掉點幅度 | 待 Week 2 |
| ch02 | D1–D5 | DP 擴展效率、ZeRO-1/2 無效性、ZeRO-3 可行性、`no_sync()` 效益、分散式正確性 | 待租到 2 卡 |
| ch06 | E1–E6 | active 比例、推論吞吐、負載均衡、中英路由差異、微調後路由偏移、EP 划算與否 | **E1、E2 ✅ 已證實** |

其中 **E4（繁中與英文的路由分布差異）與 E5（微調後路由偏移）**
超出原本 Playbook 的驗證範圍，直接連到提案 slide 10 的延伸研究方向 B。
若能證明繁中內容在 MoE 裡有可辨識的路由特徵，
「語言變體作為可控制屬性」就有了機制上的證據——建議在報告最後單獨提一頁。

---

## 附錄：待補的選讀章節

| 章節 | 為什麼 | 何時 |
|---|---|---|
| ch08 尋找最佳配置 | Week 2 消融實驗的三步驟決策流程 | **Week 2 開跑前** |
| appb 分散式效能分析 | profiler 用法 | Week 2 用 profiler 前 |
| ch00 導論、appc 典型尺度 | 補全書鳥瞰與數量級直覺 | 有空檔時 |

ch03 張量平行、ch04 上下文平行、ch05 管線平行、ch09 GPU 架構
在單卡 LoRA 實驗中用不到，Week 3 有餘裕再補。

**誠實界定範圍**：本實驗規模為 1–2 卡，碰不到大規模 DP 的通訊瓶頸，
也做不了真正的專家平行（需多卡 all-to-all）。這些章節只做理論理解，
不宣稱驗證過——這點會明確寫進最終報告。

---
