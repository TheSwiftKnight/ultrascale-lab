# 最終進度報告骨架（四週整合｜填完 `{{ }}` 後存成 final_report.md，再轉 PPTX）

> 結構刻意對齊 `ultrascale-proposal_1.pptx` 的 slide 順序：
> 每一節 = 一張（或兩張）slide。主管可以逐頁對「提案 vs 交付」。

---

## S1. 封面
繁體中文語言模型的訓練工程實證與生成範式比較——四週結案報告
曹楷萱 Kai｜2026/08｜基於 Ultra-Scale Playbook 繁中版

## S2. 提案 vs 交付對照（一頁講完「說到做到」）

| 提案承諾（slide 2/8） | 結果 |
|---|---|
| 掌握訓練工程機制並以實驗驗證 | ✅ 四週共 {{N}} 組實驗；記憶體公式誤差 0.0%（權重）、<10%（活化） |
| 微調 GPT-OSS 20B | 🔄 改為 Gemma 4 生態（主管同意）：26B-A4B 是 20B/3.6B 的規格替身 |
| AR vs DLM 對比 | ✅ 升級：DiffusionGemma = 同骨幹 26B-A4B 擴散版 → **同架構乾淨對照** |
| 4 個檢查點（兩範式 × 前後） | {{✅/⚠️ 依 DLM 微調是否降級}} |
| 評測結果表（準確率/吞吐/記憶體同表） | ✅ 見 S8 |
| 可複現程式碼 | ✅ repo + 各週執行手冊 |
| **新增**：MoE routing 主線 | ✅ 相關性（W2）→ 檢定強化（W3）→ **因果探針與介入實驗（W4）** |
| 延伸研究 / 多卡 | ✂️ 依主管指示不做（多卡章節保留理論推算） |

## S3. 四週敘事線（每週一句話）
- W1：讀通機制、資料就緒、GPT-OSS 環境驗證 → {{一句話}}
- W2：換 Gemma 4 本機實測——成本面公式全對帳（權重誤差 0.0%）；效益面微調失敗
  （−21.5 pt），診斷為格式崩潰非知識遺失
- W3：三個假設全否證的一週——超參數不是主因、Shadow-FT 前提不成立（σ=0.107）、
  微調在此設定下是淨傷害；MoE「語言專家」立論成立（繁簡 KL 僅中英 5.5%）
- W4：Week 2 真因收斂（{{P-W2 判定}}）；MoE 因果介入（{{P-E 判定}}）；
  AR vs DLM 同架構對照（{{吞吐比值}}×）

## S4. 成本面總結（Playbook 對帳，W1–W2 素材）
- 權重預測 0.0%（4-bit = 4.50 bit/param 的常數修正）
- 梯度檢查點：活化 −91%（預測 92%）；被 262K vocab 的 logits 稀釋成峰值 −30.5%
- roofline：MoE 只讀 active 參數獲實測支持（實測是 dense 假設上限的 2.79×）
- 框架對照（W3）：T4 上 Unsloth 3.7×；省的是 kernel 不是 logits（fused CE 未生效）

## S5. 效益面總結（微調路線的誠實結論）
- W2 失敗解剖：格式崩（0.68%→39.19% 無法解析）、知識面在較簡單子集持平
- W3 掃描：scaling 0.5–20 全不崩 → 超參數假設否證；所有微調 < 未微調 base（38.0%）
- W4 真因：{{P-W2a/b 判定與一段話結論}}
- 給下一階段的建議：{{微調路線去留的具體建議}}

## S6. MoE routing 主線（新增交付，兩張 slide）
**發現鏈**：負載不均（7.6×）→ 中英分工（30/30 層 p<0.01，窮舉 permutation）→
極度集中（2.9 專家蓋 80% KL）→ 語言專家非字符集專家（繁簡 KL = 中英的 5.5%）→
**W4 因果介入**：
- 方案 E 偏置探針：ΔNLL(zh) {{方向}} / ΔNLL(en) {{方向}} → {{因果判定}}
- 方案 B router-only vs 方案 A attention-only：{{機制二分結論：路由問題 or 能力問題}}
- 英文對照組：{{en 掉/不掉 → 分工可操作性判定}}
- 成本語言：方案 C（選擇性 expert LoRA，15.2M 參數、比 attention 全掛僅貴 30%）
  為 future work——本機優化器狀態裝不下，但挑專家規則（繁簡交集）已驗證穩定（88%）

## S7. AR vs DLM（proposal 的招牌對比）
- 對照設計：同骨幹 26B-A4B，唯一變因 = 生成範式
- 吞吐：AR {{53.6}} vs DLM {{x}} tok/s（M4/4-bit）→ 比值 {{r}}×；
  對照官方 4×：{{roofline 解讀——M4 batch=1 算力飽和，落差可解釋}}
- 品質（TMMLU+ 嚴格 micro）：{{AR base}} / {{DLM base}} / {{AR tuned}} / {{DLM tuned}}
- 記憶體：權重佔用幾乎同（同一組權重）；差在 canvas activations {{Δ GiB}}
- 回答 slide 9 三問：{{每問兩三句}}

## S8. 評測主表（slide 9 的最終版）
{{Step 6 主表貼這裡，含可比性注腳}}

## S9. 方法論收穫（給團隊的可複用清單）
1. 設定檔不是事實，產出的檔案才是（k/v 靜默略過、scale 語意）
2. 前提先驗（σ 檢驗一小時省一週）
3. 事前寫預測與分支（「全都不崩」才能被認出是訊號）
4. 每個量測工具配一個獨立對帳檢查
5. 檢查要確定性（set 順序、種子、macro/micro 並列）
6. {{W4 新增}}

## S10. 限制與 future work
- 方案 C 實作、翻譯配對 200 句與語碼混用對照、生成期路由、多卡 DP/ZeRO、
  P1（bf16 基準線）/P2（FA on/off）{{若未跑}}、DLM {{若降級的補做計畫}}

## S11. 附錄
各週執行手冊/總結、repo 結構、可複現指令、假設狀態總表（H/E/S/P/Q/W2/E/A/B 全編號）
