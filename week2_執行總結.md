# Week 2 執行總結 —— 進度報告講稿

## 0. 大綱

- 實驗改在我的本機跑，所以模型從 GPT-OSS 換成 Gemma 4。
    - 重算預測值
- 跑完**九組成本面實驗**＆TMMLU+ **的微調前後各一輪，各 1,036 題。**

兩句話總結：

1. **成本面（記憶體／吞吐）**：Playbook 的公式方向全對，但直接代入會系統性高估；把架構細節與量化格式的細節補進去之後，**權重預測和實測的誤差是 0.0%**，活化在 10% 以內。
2. **效益面（準確率）**：**這次微調是失敗的——但失敗的原因不是知識沒學到，是輸出格式的遵循被洗掉了。**逐題加權的正確率從 **59.75% 掉到 38.22%（−21.5 pt）**，同時**無法解析率從 0.68% 暴增到 39.19%**。做完逐題控制對照之後可以確定：**掉的分數幾乎全部來自格式，不是來自答題能力。**而最大的嫌疑犯是一個設定值——我們的 LoRA 強度是業界常規的 **10 倍**（見 4.3）。

---

## 1. 換了哪兩個模型，為什麼是這兩個

換到 Gemma 4 之後，我挑的是這兩個：

- **Gemma 4 E4B**（dense）—— `mlx-community/gemma-4-e4b-it-4bit`
- **Gemma 4 26B-A4B**（MoE）—— `mlx-community/gemma-4-26B-A4B-it-4bit`

| | E4B dense | 26B-A4B MoE |
|---|---:|---:|
| 總參數 | 7.46B | 25.23B |
| **每 token 實際用到** | **3.97B**（非嵌入） | **3.82B**（active） |

**每 token 的計算量幾乎一樣，總參數差 3.4 倍。**

- **E4B 的 bf16 只有 13.9 GiB，我的機器塞得下**。26B 的 bf16 是 47 GiB，本機不行。

---

## 2. 這週跑了哪些實驗

**第一組——記憶體（H1、H3、H4、H5、H6）**
Playbook ch01 記憶體公式是為標準 Transformer 寫的。Gemma 4 有滑動視窗注意力、GQA、後 18 層共用 KV、Per-Layer Embeddings，這些都不在公式裡。

**第二組——MoE 相關（E2、E3、E4、E5）**
MoE 宣稱「只算 active 參數」。這句話如果是真的，應該可以從**記憶體頻寬**上被觀測到，另外 MoE 的路由器是黑盒子，我想知道它在繁中和英文上是不是真的走不同的路。

**第三組——準確率（TMMLU+）**

---

## 3. 數字與解讀

### 3.1 H1：記憶體預測 vs 實測 —— 公式可信

| 模型 | 預測（4-bit） | 載入後實測 | 誤差 | 生成峰值 |
|---|---:|---:|---:|---:|
| E4B (dense) | 3.91 GiB | 3.91 GiB | **−0.0%** ✅ | 4.02 GiB |
| 26B-A4B (MoE) | 13.22 GiB | 13.23 GiB | **−0.0%** ✅ | 13.37 GiB |

**兩個模型的權重預測都準到小數點後一位。**這件事的關鍵在一個常數：MLX 的 4-bit（group_size=64）是 4 bit 權重加上每 64 個元素一組的 scale 和 bias，而 **scale 和 bias 都是 bf16**，所以等效 **4 + (2+2)×8/64 = 4.50 bit/參數**，不是直覺上的 4.25（那是 group_size 128 的值）。直接量 `out/gemma4-e4b-tw/model.safetensors` 得到 4.501，和公式吻合。

**這是本週最重要的方法論收穫之一**：記憶體公式本身沒問題，會算錯是因為量化格式的細節。同樣叫「4-bit」，group_size 不同就差 5.6%。

生成峰值比權重高出的部分（E4B +2.8%、26B +1.1%）是 KV cache、Metal buffer pool 與 logits。E4B 因為 GQA 只有 2 個 KV 頭，KV cache 幾乎可以忽略。

---

### 3.2 E2：記憶體頻寬 roofline 

解碼階段：每產生一個 token，用到的權重就得從記憶體讀一次。所以理論上限 = 頻寬 ÷ 每 token 讀取量。我的機器是 273 GB/s。

第一次算出來的結果是這樣：

| 模型 | 每 token 讀取 | 理論上限 | 實測 | 達成率 |
|---|---:|---:|---:|---:|
| E4B | 4.20 GB | 65.0 tok/s | 69.1 tok/s | **106%** ⚠️ |
| 26B-A4B | 2.84 GB | 96.1 tok/s | 53.6 tok/s | 56% |

**106% 物理上不可能**

發現：E4B 的 7.46B 參數裡，有 **2.82B 是 Per-Layer Embeddings，佔全模型 38%**，這些每個 token 只讀其中一列，不是讀整張embedding table。

修正之後：

| 模型 | 修正後每 token 讀取 | 理論上限 | 實測 | 達成率 |
|---|---:|---:|---:|---:|
| E4B | 2.34 GB | 116.4 tok/s | 69.1 tok/s | **59%** ✅ |
| 26B-A4B | 2.45 GB | 111.5 tok/s | 53.6 tok/s | **48%** ✅ |

證明MoE只讀active param：如果 26B-A4B 是 dense，每 token 要讀 14.20 GB，理論上限只有 19.2 tok/s。**實測 53.6 是它的 2.79 倍。**

---

### 3.3 dense vs MoE 對照

| 指標 | E4B dense | 26B-A4B MoE | 比值 |
|---|---:|---:|---:|
| 權重實際佔用 | 3.91 GiB | 13.22 GiB | **3.4×** |
| 生成峰值 | 4.02 GiB | 13.37 GiB | 3.3× |
| 載入耗時 | 6.8 s | 10.3 s | 1.5× |
| 生成吞吐 | 69.1 tok/s | 53.6 tok/s | **0.78×** |

在每 token 計算量相同的前提下，MoE 9.3 GiB 額外記憶體跑出的速度反而更慢，因為 MoE 有 gating 和 gather 的額外開銷--> 準確率應該要更高。（ref. TMMLU+ 準確率）

---

### 3.4 H3：梯度檢查點 (gradient checkpointing)

（設定：E4B、seq=2048、batch=1、各跑 30 步取後半平均）

| | 峰值記憶體 | s/step | tokens/s | train loss |
|---|---:|---:|---:|---:|
| 不開 | 10.59 GiB | 2.703 | 315 | 1.8033 |
| 開 | 7.37 GiB | 3.219 | 264 | 1.8033 |
| **變化** | **−30.5%** | **+19.1%** | −16% | 完全相同 |

峰值記憶體中：
| 項目 | 不開 | 開 | 說明 |
|---|---:|---:|---|
| 權重 + LoRA | 4.05 | 4.05 | 不受影響 |
| logits（2048 × 262,144） | 3.00 | 3.00 | 不受影響 |
| **活化（反推）** | **3.54** | **0.32** | **−91.0%** |
| 合計 | 10.59 | 7.37 | −30.5% |

**把不受影響的兩塊扣掉，活化本身降了 91.0%，和預測的 92% 重合。**

結論：**梯度檢查點只對活化有效，而活化在這個配置下只佔峰值的三分之一——所以它的邊際效益取決於你的 vocab 和權重佔多大比重。**Gemma 4 的 262K vocab 讓 logits 吃掉 3 GiB，這件事把梯度檢查點的效果稀釋掉了。

---

### 3.5 H4：序列長度 

| max_seq_length | 峰值記憶體 | s/step | train loss |
|---:|---:|---:|---:|
| 512 | 5.52 GiB | 1.886 | 1.9247 |
| 1024 | 6.79 GiB | 3.071 | 1.8077 |
| 2048 | 7.37 GiB | 3.222 | 1.8033 |
| 4096 | **7.37 GiB** | 3.226 | **1.8033** |

2048 和 4096 這兩列：**峰值一樣、耗時一樣、loss 一樣到小數第四位。**

查了資料的 token 長度分布後：

| mean | p50 | p90 | p95 | p99 | max |
|---:|---:|---:|---:|---:|---:|
| 848.6 | 848 | 1250 | 1380 | 1657 | **2581** |

**整份資料最長的樣本是 2581 token，99.95% 的樣本在 2048 以內。**而這 30 步抽到的樣本剛好全部短於 2048——所以 2048 和 4096 這兩組跑的是**相同、沒有被截斷的資料**。

**解讀**：`max_seq_length` 應該用**資料的 p99 或 max** 來設。設成 4096 在這份資料上沒有好處

---

### 3.6 H5：batch size —— 本機沒有 scaling 空間

（設定：E4B、seq=512、開檢查點）

| batch | 峰值記憶體 | s/step | **tokens/s** |
|---:|---:|---:|---:|
| 1 | 5.52 GiB | 1.889 | 271 |
| 2 | 6.76 GiB | 3.759 | 263 |
| 4 | 9.01 GiB | 7.481 | 255 |
| 8 | 13.56 GiB | 15.075 | **257** |

「記憶體：+145.7%（2.46 倍）。看起來不像線性——但扣掉(相對於batch size)固定的 4.05 GiB 權重之後，可變部分是 **1.47 → 9.51 GiB，成長 6.5 倍**，接近 batch 的 8 倍。（**扣掉固定項後 H5 成立。**）

**吞吐從 271 掉到 257，沒有提升。**

每步耗時是 **7.98 倍**——**幾乎線性**。

**怎麼解讀**：在一般 GPU 上，加大 batch 是為了把閒置的算力填滿，所以吞吐會上升。這裡完全沒有上升，代表我的本機(M4 Pro) 在 batch=1 的時候就已經吃滿算力了。

---

### 3.7 H6：LoRA 掛幾層 

（設定：E4B、seq=2048、bs=1、開檢查點）

| LoRA 層數 | 峰值記憶體 | s/step | tokens/s | **train loss** |
|---:|---:|---:|---:|---:|
| 4 層 | 7.27 GiB | 2.421 | 352 | 1.9920 |
| 16 層 | 7.37 GiB | 3.215 | 264 | 1.8033 |
| 全部 42 層 | 7.67 GiB | 5.181 | 164 | **1.5873** |

**記憶體只增加 5.4%，時間增加 114%，loss 明顯更好。**

---

### 3.8 E3 / E4 / E5：MoE 的路由器在做什麼

26B-A4B 有 30 層 × 128 個路由專家 = **3,840 組專家矩陣**，每個 token 只走其中 8 個。我在 router 上掛了攔截，記錄每個 token 實際選了誰。

**E3：專家負載均不均衡？**

| 指標 | 數值 | 對照 |
|---|---:|---|
| 專家使用率的變異係數 (cv) | **1.89** | 隨機雜訊底線 0.25 → **訊噪比 7.6×** |
| 低使用專家數 | **1,565 / 3,840**（40.8%） | |
| 最熱門專家佔比 | **10.62%** | 理想值 0.78% → **13.6 倍** |

**明顯不均衡。**

**E4：中文和英文走不同的專家嗎？**

| 指標 | 數值 |
|---|---:|
| KL(中文路由 ‖ 英文路由) 平均 | **0.7042** |
| 雜訊底線（bootstrap，300 次重抽） | 0.1063 |
| **訊噪比** | **6.6×** |
| p < 0.01 的層數（add-0.5 平滑 KL／JSD） | **29 / 30** |

雜訊底線不能用解析式 `(K−1)/(2N) = 0.0306`——那是 N ≫ K 的漸近展開，本例 N/K ≈ 16 且四成專家幾乎沒被選到，條件不成立，會低估 3.5 倍。改用參數化 bootstrap 直接量（`scripts/analyze_router_lang.py`）。

路由器確實學到了語言層級的分工，而且**極度集中**：平均只要 **2.9 個專家**就涵蓋單層 80% 的 KL；全模型 3,840 個專家裡只有 128 個是明確中文傾向，其中 26 個在英文樣本裡一次都沒被選到。這對繁中微調是好消息——代表有一小群專家是「處理中文的」，微調可以針對性地改善它們，成本比想像中低很多。細節見 `moe_routing_分析.md`。

**E5：微調之後路由變了嗎？**

我用 26B 跑了 200 步 LoRA（只掛最後 8 層的 attention；設定寫 `q/k/v/o_proj`，但 full_attention 層沒有獨立的 `v_proj`，實際 adapter 是 3.09M），然後重測路由：

| 層 | KL(微調前 ‖ 微調後) |
|---|---:|
| 第 0–21 層 | **全部 0.000** |
| 第 22–27 層 | 0.171 – 0.508 |
| 第 28 層 | 2.065 |
| 第 29 層 | **6.327** |

1. **前 22 層完全沒變，後 8 層變了——而後 8 層剛好就是 LoRA 掛上去的那 8 層。**

2. 沒有訓練roter本身：LoRA 只掛在 attention 的四個投影上。但 router 的行為還是變了，因為**餵給 router 的 hidden state 被 attention 改變了**。這說明 MoE 的路由會隨著上游的改動而漂移。

3. **KL 隨層數暴增**（第 29 層是第 22 層的 37 倍）。越接近輸出，路由改變越劇烈。

怎麼解讀：如果之後要對 MoE 做繁中微調，**只微調 attention 就已經足以改變路由行為**，不需要去動 expert 本身（LoRA 全掛，可訓練參數會從 attention-only 的 11.5M 變成 667.4M，**膨脹 58 倍**，優化器狀態要 9.9 GiB）。對照組是 E4B dense，全掛只膨脹 3.8 倍。

不過如果真的要動專家，也不必全掛：E4 顯示中英路由的差異集中在極少數專家上，每層只取貢獻最高的 3 個掛 LoRA 只需 15.2M 參數（0.23 GiB）。實作路徑見 `moe_routing_分析.md` §5。

---

## 4. TMMLU+ 準確率

base用Gemma 4 E4B Dense 4-bit，tuned用相同模型加上訓練 1,000 步的 LoRA融合進模型

微調前後各一輪、各 1,036 題，合計 2,072 題

### 4.1 主結果：逐題正確率掉了 21.5 個百分點

（設定：三科 smoke 子集共 1,036 題，`box` 計分、`shuffle_options=true`、`temperature=0`、`max_tokens=2048`、`enable_thinking=true`。**微調前後兩份 config 只有 `model.name` 一行不同。**）

| | 微調前 base | 微調後 tuned | 變化 |
|---|---:|---:|---:|
| **正確率（逐題加權，micro）** | **59.75%** | **38.22%** | **−21.5 pt** ❌ |
| 正確率（科目平均，macro） | 53.98% | 36.57% | −17.4 pt |
| 無法解析題數 | 7 / 1,036 | **406 / 1,036** | **× 58** |
| 無法解析率（micro） | 0.68% | **39.19%** | **+38.5 pt** |
| 評測耗時 | 59.6 分 | 55.8 分 | — |

**兩個平均要分開報。**twinkle-eval 的 `average_accuracy` 是對「科目」取平均，但三科題數是 768 / 139 / 129 —— 台語（129 題）和台灣地理（768 題）在總分裡權重相同。**逐題加權才是實際的答題正確率，跌幅是 21.5 pt。**後續一律兩個都報。

另一個要注意的：twinkle-eval 的 `shuffle_options` 用的是**沒有設種子的全域 `random`**（整個套件裡沒有任何 `random.seed()`）。所以 base 那一輪和 tuned 那一輪看到的選項順序是不同的，這個差距裡含有一部分排列造成的雜訊。之後的評測要自己固定種子。

### 4.2 

微調後 406 題無法解析的輸出分類：

| 類型 | 題數 | 佔比 |
|---|---:|---:|
| **有完整答案，但用散文寫**（「答案是 D，因為…」） | **318** | **78.3%** |
| 生成到 2,048 token 上限被截斷 | 86 | 21.2% |
| 有 `\box{}` 但裡面塞的不是選項字母 | 2 | 0.5% |

Ex. 
- `答案是 D，因為臺灣的政治民主是其發展歷程中的一個獨特且值得深入探討的方面…`
- `B` ← 就一個字母，沒有 box
- `\box{社會及工業改革}` ← 把選項**內容**而不是**字母**塞進去

只看「有照格式輸出」的那些題：

| | 解析成功題數 | 這些題上的正確率 |
|---|---:|---:|
| base | 1,029 | 60.16% |
| tuned | 630 | **62.86%** |

看起來微調確實好了 +2.7 pt，但兩邊的分母是不同的題目無法比較。

而且那 630 題是**比較簡單的一群**：base 在這 630 題上得 64.60%，在 tuned 崩掉的 406 題上只得 52.22%。所以「知識面持平」這個結論的正確講法是「**在較簡單的子集上知識沒掉**」，不能直接推論到全體。

---

### 4.3 兩個根因：訓練資料沒有這個格式，而且 LoRA 開太強

| 訓練設定 | 值 |
|---|---|
| 資料 | `twinkle-ai/tw-reasoning-instruct-50k`，抽 8,000 筆（train 7,600 / val 400） |
| 形態 | 長篇 CoT 推理 + **散文式回答**，**沒有任何一筆是 `\box{X}` 這種受限格式** |
| LoRA 設定值 | `num_layers: 16`（最後 16 層）、`rank: 16`、`scale: 20.0`、lr 1e-4 cosine、1,000 步 |
| **實際訓練到的** | **只有 q_proj + o_proj，2,555,904 個參數** |
| **等效 LoRA 強度** | **scaling = 20.0**（= HF PEFT 的 `lora_alpha` 320） |

最後兩列需要解釋，因為它們和設定檔的字面意思不同：

**（a）`k_proj` / `v_proj` 一個都沒訓練到。**解析 `out/lora-e4b/adapters.safetensors` 的 header：64 個 tensor，全部是 `layers.{26..41}.self_attn.{q_proj,o_proj}`。原因是 E4B 的 `num_kv_shared_layers = 18`，layer 24–41 共用前面算好的 K/V，**這些層沒有 `k_proj` / `v_proj` 模組**，而掛的最後 16 層（26–41）完全落在裡面。mlx-lm 對找不到的 key 是靜默略過的。

**（b）LoRA 強度是業界常規的 10 倍。**`mlx_lm/tuner/lora.py`：

```python
return y + (self.scale * z).astype(x.dtype)     # scale 直接乘，沒有除以 rank
```

`scale` 是**直接乘數**，不是 HF PEFT 慣例的 `lora_alpha / r`。`scale: 20.0`（mlx-lm 的預設值）等效 PEFT 的 `lora_alpha = 320`，而業界常用的是 `alpha=32, r=16` → scaling 2.0。

**LoRA 更新量被放大 10 倍，等於用一個非常大的步伐去覆寫原模型的行為 —— 這正是「格式遵循被洗掉、知識沒掉」這種失敗長相的典型成因。**所以 4.4 的修法要先驗證這一項，而不是先怪模型。

生成長度的變化直接支持這個說法：

| 完成 token | base | tuned |
|---|---:|---:|
| 平均 | 649 | 606 |
| 中位數 | 564 | **411** |
| p90 | 1,076 | **1,541** |
| 撞到 2,048 上限 | 4 題（0.4%） | **86 題（8.3%）** |

**分布從單峰變成雙峰**：中位數變短（答得更乾脆、但不套格式），p90 和截斷率卻大幅上升（另一群題目開始寫不完）。這正是「訓練資料的輸出風格覆蓋掉原本的 instruction-following」的典型長相。

"災難性遺忘（catastrophic forgetting）"：

1. 格式崩了（無法解析 0.68% → 39.19%）
2. 知識在較簡單的子集上持平（控制對照 −1.7 pt）
3. 訓練資料裡沒有該格式（100% 散文）
4. 生成長度分布同向改變（雙峰化、截斷率 ×21）
5. **LoRA 強度是常規的 10 倍**（scaling 20 vs 2）

---

### 4.4 怎麼修

| 修法 | 具體做法 | 為什麼 |
|---|---|---|
| **A. 降低 LoRA 強度**（最高優先） | `scale` 20 → 2，掃 0.5 / 1 / 2 / 4 / 20 五個點 | 4.3(b) 指出這是最大的嫌疑犯，而且是**唯一一個不用改資料、不用改模型**就能驗證的因素 |
| **B. 掃 checkpoint** | 200 / 400 / 600 / 800 / 1000 五個 adapter 各跑一次 smoke 評測 | **不用重訓**（`save_every: 200`，五個檔案都在 `out/lora-e4b/`），可畫出「訓練步數 vs 格式保留率」曲線。和 A 是不同的軸 |
| **C. 資料混入格式樣本** | 從 TMMLU+ **非評測科目**抽 5–10% 題目，照 `\box{X}` 做成 SFT 樣本混進去 | 讓模型同時看過散文和受限格式 |
| **D. Shadow-FT** | 在 base（`-pt`）上訓練 LoRA，把同一個 adapter 掛到 instruct 上 | 論文（arXiv 2505.12716）主張 instruct 模型的 post-training 先驗會抵抗 SFT，在 base 上學可以避開。Gemma-3-4B 是它效果最顯著的案例 |
| **E. 評測報四個數字** | 嚴格 `box` + 寬鬆解析 × macro + micro | 分辨掉的是知識還是格式，以及是不是被科目權重放大 |
| **F. 固定評測種子** | 自己實作 `shuffle_options`，種子寫死 | twinkle-eval 用的是沒設種子的全域 `random`，兩輪之間題目排列不同 |

---

## 5. 下週規劃

| 優先序 | 項目 | 產出 | 在哪跑 |
|---|---|---|---|
| **P0** | **LoRA scaling 掃描**（0.5 / 1 / 2 / 4 / 20），每組接一次評測 | **「LoRA 強度 vs 格式保留率」曲線** | Colab |
| **P0** | **Shadow-FT**：在 `gemma-3-4b-pt` 上訓練，adapter 搬到 `-it` | 2×2 表，把「超參數」和「post-training 脆弱性」分開 | Colab |
| P0 | 掃 200/400/600/800/1000 五個 checkpoint 的 smoke 評測（adapter 已存在，**不用重訓**） | 「訓練步數 vs 格式保留率」曲線 | 本機 |
| P1 | 評測腳本改成自製、種子固定，同時報 macro/micro × 嚴格/寬鬆 | 之後每次都能分辨掉的是知識還是格式 | Colab |
| P1 | 框架對照：HF transformers+peft vs Unsloth，同參數同步數 | 峰值記憶體／s per step／loss 的量化差距 | Colab |
| P1 | 訓練資料混入 5–10% `\box{}` 格式樣本，重訓一次 + 重評 | 一個**不會洗掉格式**的 dense 配方 | Colab |
| P1 | **配方確認可用之後**，26B-A4B 才跑完整 1,000 步 + 融合 + 評測 | **dense vs MoE 的完整對照表** | 本機（貼邊，需先放寬 wired limit） |
| P2 | 三科 smoke 確認配方後，擴到 10 科 `tmmluplus_tw10` | 較穩定的準確率數字 | Colab |
| P2 | E4B bf16 vs 4-bit 準確率對照（P1/P4） | 量化到底損失多少 | 本機（bf16 13.9 GiB 塞得下） |
| P2 | 補 `notes/ch05.md` | 筆記一致性 | — |
| P2 | Flash Attention on/off 對照（MLX 沒有開關，只能在 CUDA 做） | 假設 P2 | CUDA（Colab Pro 以上） |

詳細的執行步驟見 `week3_執行手冊.md`。

---

## 6. 是否需要 CUDA GPU 

- **本機沒有「利用率」這個維度可以觀測。**
剛剛 H5 那張表：batch 從 1 加到 8，吞吐從 271 掉到 257，代表 M4 Pro 在 batch=1 就吃滿了

- **Flash Attention 的 on/off 對照在 MLX 上不存在。**
P2 這個假設是「Flash Attention 讓 S/P 矩陣不落地，省下多少活化」。MLX 的 attention 實作是寫死的，**沒有開關可以切**。PyTorch 可以用 SDPA 的 backend context manager 明確切換，這個對照只有在 CUDA 上做得出來。

- 26B 的 bf16 在本機跑不了
47.0 GiB，我的機器是 24 GB。**所有 4-bit 量化的結論，目前沒有 bf16 的基準線可以對照。**「4-bit 到底損失多少」這個問題，在 26B 上本機答不了。

---

## 7. 收尾

1. **公式是對的，錯的是代進去的常數。**權重預測誤差 0.0%，但前提是量化格式要算對（group_size 64 的 MLX 4-bit 是 4.50 bit/參數，不是 4.25）。活化的絕對值高估 2.4 倍，roofline 的分母一開始整個算錯。**Playbook 是好的起點，但每一個常數都要回頭對實際檔案。**

2. **這週抓的 bug**：roofline 把查表當矩陣乘、專家均衡的理想值差 8 倍、路由攔截掛在實例上不會觸發、評測「請求全部 200 但零解析」、KL 的雜訊底線用了不適用的漸近式、量化位元數少算 0.25 bit。**其中大部分會安靜地產生看起來合理的結果。**所以我後來給每個量測工具都加了一個獨立的對帳檢查——「攔截到的層數必須等於模型層數」「選擇次數必須等於 token 數 × top_k」「正式評測前先送一題並斷言抽得出正解」。**這是這週方法論上最大的收穫。**

3. **設定檔寫的和實際跑的不一定一樣。**兩個都是解析 `adapters.safetensors` 的 header 才發現的：`k_proj`/`v_proj` 一層都沒訓練到（掛的層剛好都在 KV 共用區）；`scale: 20.0` 是直接乘數不是 alpha/rank，等效強度是常規的 10 倍。**設定檔不是事實，產出的檔案才是。**

4. **成本這半邊量完了；效益這半邊也量完了，而且答案是負的。**微調後逐題正確率 −21.5 pt，但拆開來看是**格式崩掉**（無法解析 0.68% → 39.19%），逐題控制對照顯示**在較簡單的子集上知識持平**（−1.7 pt）。**這是災難性遺忘，不是模型或流程壞掉。**

5. **下週的重點從「補數字」變成「修配方」。**優先驗證 LoRA 強度這一項（不用改資料、不用換模型），再試 Shadow-FT。

---

# 附錄：本週產出檔案

| 檔案 | 內容 |
|---|---|
| `reports/memory_prediction_gemma.md` | 兩個模型的完整記憶體預測（ch01 + ch10 公式推算） |
| `reports/load_verification_gemma.md` | H1 預測 vs 實測、E2 roofline |
| `reports/ablation_checkpoint.md` | H3 |
| `reports/ablation_seqlen.md` | H4 |
| `reports/ablation_batch.md` | H5 |
| `reports/ablation_lora.md` | H6 |
| `reports/router_before.json` | E3 專家負載、E4 中英路由 KL |
| `reports/router_after.json` | E5 微調前後路由 KL |
| `reports/router_lang_analysis.json` | E4 的 bootstrap 檢定、逐層 p-value、專家層級 KL 貢獻、選擇性微調的 LoRA 預算 |
| `reports/data_stats_gemma.json` | 資料管線五階段統計、token 長度分布 |
| `out/lora-e4b/` | E4B LoRA adapter（1000 步） |
| `out/lora-26b/` | 26B LoRA adapter（200 步） |
| `out/gemma4-e4b-tw/` | E4B 融合後權重（4.2 GB，評測用） |
| `notes/ch01.md, ch02.md, ch06.md, ch10.md` | 章節筆記，數字已全面換成 Gemma 4 |
| `results/results_20260814_0015.json` | **TMMLU+ base 結果**（micro 59.75% / macro 53.98%，無法解析 7 題） |
| `results/results_20260814_0637.json` | **TMMLU+ tuned 結果**（micro 38.22% / macro 36.57%，無法解析 406 題） |
| `results/eval_results_*_run0.jsonl` | 逐題原始輸出（各 1,036 筆，含 reasoning） |
| `scripts/run_eval.sh` / `scripts/probe_eval.py` | 評測前三關防呆（proxy／model id／端到端抽答案） |
| `scripts/analyze_eval.py` | **第 4 節所有拆解數字的來源**（格式成因分類、逐題控制對照、寬鬆解析重算），一行指令可重現 |
| `scripts/analyze_router_lang.py` | E4/E5 的二次分析（bootstrap 雜訊底線、逐層 p-value、專家層級 KL 貢獻、LoRA 預算表） |
| `moe_routing_分析.md` | MoE 路由專篇：E3/E4/E5 的完整方法論與四個微調提案 |
| `week2_執行手冊.md` | Step 0–11 的完整操作手冊 |

**接續 Week 3**：LoRA scaling 掃描、Shadow-FT、框架對照、五個 checkpoint 的格式保留率曲線、26B 的完整評測、`notes/ch05.md`。執行細節見 `week3_執行手冊.md`。
